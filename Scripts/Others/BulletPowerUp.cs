using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletPowerUp : MonoBehaviour
{/*
    public enum Upgrade
    {
        bullet,
        especial
    }

    public Upgrade _upgrade;

    public void PlayerOneUpdate()
    {
        if(_upgrade == Upgrade.especial)
        {
            PlayerOne.instance.EspecialUpgrade();
        }else if(_upgrade == Upgrade.bullet)
        {
            PlayerOne.instance.BulletUp();
        }
    }


    public void PlayerTwoUpdate()
    {
        if (_upgrade == Upgrade.especial)
        {
            PlayerTwo.instance.EspecialUpgrade();
        }
        else if (_upgrade == Upgrade.bullet)
        {
            PlayerTwo.instance.BulletUp();
        }
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.CompareTag("Player"))
        {
            PlayerOneUpdate();
            Destroy(gameObject);
        }
        
        if (collision.CompareTag("Player2"))
        {
            PlayerTwoUpdate();
            Destroy(gameObject);
        }
    }*/
}
